# Copy to .env and fill in. .env is gitignored.
HF_TOKEN=            # Hugging Face token: read for the gated dataset; write to push dataset/model repos
GEMINI_API_KEY=      # Google AI Studio key: data enrichment + generation (steps 2–3)
VOICE_AGENT_BACKEND= # optional: path to the voice-agent backend/ dir, for the cascade comparison column
OPENROUTER_API_KEY=  # OpenRouter key: Jev (typesafe/jev-1.13) comparison, scripts/12_jev_compare.py
